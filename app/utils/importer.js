// npm import thisfile.json
  // >> node ./app/utils/importer.js thisfile.json
  // store before length

console.log(process.argv[2]);
// fs read process.argv[2] >> for each
  // updatedMovie = infoFetcher.fetch(movie.title);
  // >> var movie = new Movie { title: updatedMovie.title, updatedMovie.released}
  // movie.save
// get after length
// console log complete + before length, after length
